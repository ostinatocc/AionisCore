/** Re-exports from `@aionis/ui-kit/lib` — the canonical alias map. */
export {
  alias,
  displayOf,
  toneOf,
  normalizeBadgeTone,
  type AliasEntry,
  type AliasTone,
  type BadgeTone,
} from "@aionis/ui-kit/lib";
