/**
 * Deprecated: re-exports from `@aionis/ui-kit`. New code should import from
 * `@aionis/ui-kit/lib` directly. This shim exists so in-tree Playground
 * components don't churn while the package ships.
 */
export {
  alias,
  displayOf,
  toneOf,
  normalizeBadgeTone,
  type AliasEntry,
  type AliasTone,
  type BadgeTone,
} from "@aionis/ui-kit/lib";
