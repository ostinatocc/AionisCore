# Lite Repo Baseline Notes

This repository was initially generated by:

```bash
/Users/lucio/Desktop/Aionis/scripts/bootstrap-lite-repo.sh --dest /Volumes/ziel/Aionisgo
```

What was originally copied:

1. current `src/` runtime tree
2. a thin Lite app launcher under `apps/lite/dist/`
3. `apps/lite/` wrapper app
4. `packages/runtime-core/` boundary package
5. Lite startup shim and runtime manifests
6. Lite EN/ZH public-beta docs

What was intentionally generated during extraction:

1. standalone-lite `package.json`
2. Lite-only CI workflow
3. minimal release workflow
4. Lite smoke script
5. Lite startup contract test
6. Lite local-identity defaults via `LITE_LOCAL_ACTOR_ID`

Remaining release-baseline work:

1. trim copied `src/` down to the true Lite/shared-core subset
2. decide whether shared runtime-core stays vendored or published
3. curate the first public commit boundary
4. add release notes and issue triage policy
5. decide whether Lite local identity should remain single-actor only or grow named local profiles
